export type User = {
    id: number,
    name: string,
    pseudo: string,
    label:string
}

export type UserDetails = {
    id: number,
    name: string,
    pseudo: string,
    score: number,
    label: string
}